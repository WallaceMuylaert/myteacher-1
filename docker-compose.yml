services:
  db:
    image: postgres:15-alpine
    container_name: myteacher_db
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - myteacher_data:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}" ]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: myteacher_backend
    restart: always
    depends_on:
      db:
        condition: service_healthy
    env_file:
      - .env
    environment:
      - DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
      - PYTHONUNBUFFERED=1
    volumes:
      - ./backend:/app/backend:rw
      - ./database:/app/database:ro
    command: uvicorn backend.server:app --host 0.0.0.0 --port 8501
    ports:
      - "127.0.0.1:${PORT_BACKEND:-8501}:8501"

  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    container_name: myteacher_frontend
    restart: always
    depends_on:
      - backend
    environment:
      - CHOKIDAR_USEPOLLING=true
      - WATCHPACK_POLLING=true
      - VITE_API_URL=${VITE_API_URL:-http://localhost:8501}
      - VITE_SUPPORT_WHATSAPP_NUMBER=${VITE_SUPPORT_WHATSAPP_NUMBER}
    volumes:
      - ./frontend:/app/frontend:rw
      - /app/frontend/node_modules
    ports:
      - "${PORT_FRONTEND:-5273}:5273"

  pgadmin:
    image: dpage/pgadmin4
    container_name: myteacher_pgadmin
    restart: unless-stopped
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_DEFAULT_EMAIL}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_DEFAULT_PASSWORD}
    ports:
      - "127.0.0.1:5150:80"
    depends_on:
      - db
    profiles:
      - tools

volumes:
  myteacher_data:
